# 领域词汇表

本文档只记录项目领域语言，不记录实现细节。

## 参考图组

一组可复用的参考素材集合，用来表达“哪些图片应该以什么方式影响生成结果”。参考图组可以被生成任务使用，但生成任务不强制依赖参考图组。

## 参考意图

参考图组中的单条参考规则。它描述某张参考图的用途，例如主体、风格、构图或元素，以及这条参考在生成中的权重、启用状态和备注。

## 生成任务

画布上的显式生成入口。生成任务用于把一次生成操作作为可排队、可追踪、可重试、可取消的任务来管理。当前产品界面优先用于图片生成，未来可以扩展到文本、音频和视频。

生成任务的提示词是必填项；参考图组是可选项。生成任务可以自动读取上游连线内容，并允许用户在节点内手动覆盖。图片生成任务的多个候选结果归入同一个图片组。

## 生成运行

一次真实模型执行记录，表示从创建、排队、执行到成功、失败或取消的完整生命周期。当生成任务需要多个候选图时，可以对应多个生成运行。

## 图片组

一次图片生成产生的候选图片集合。图片组强调这些候选属于同一生成意图，可以作为一个整体展开、收起、复用或继续生成。
